name: GitHub Actions Demo
on: [pull_request]
jobs:
  Explore-GitHub-Actions:
    runs-on: ubuntu-latest
    steps:
    - name: Set variables
      id: vars
      run: |
        echo ::set-output name=publish::false
        echo ::set-output name=version::1
    - name: read variables
      id: read
      run: |
        echo ::set-output name=deployed::$([ -z "${{ steps.vars.outputs.version }}" ] && echo "UNO" || echo "VACIO")
    - name: print variables
      run: |
        echo "deployed ${{ steps.read.outputs.deployed }}"
